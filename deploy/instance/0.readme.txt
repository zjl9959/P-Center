instances文件夹中：

1、*.txt文档内容为：
   第一行依次是：结点数、边数、服务节点数p
   接下来每行为：结点i、结点j、结点i到结点j之间的距离
  
注：本文档给出了一个无向连通图，任意两点直接的距离已知，或者可以间接算出来，比方用FLOYD最短路径算法。



2、*.tsp文档内容为：
   第一行是：结点数
   接下来每行是：结点号、x坐标、y坐标
   最后一行EOF结束
  
注：本文档给出了每个结点的坐标，任意两点间的距离可以用坐标计算。